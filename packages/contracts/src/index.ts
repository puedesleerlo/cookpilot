/**
 * The wire contract between the web client and the API.
 *
 * Request and response schemas live here rather than in either side, so the client cannot
 * drift from the server by editing its own copy. Populated by `add-api-foundation`.
 */
export const API_VERSION = 'v1';
